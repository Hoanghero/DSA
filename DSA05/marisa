#include <stdio.h>

int main()
{
    int n, k;
    scanf("%d %d \n", &n, &k);
    if(n <1 || k> n || n > 20) return 0;

    for(int i=1; i<n; i++ )
    {
        for(int j=i+1; j<=n; j++)
        {
            printf("%d %d \n", i, j);
        }
    }
    return 0;
}